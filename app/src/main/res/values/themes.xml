<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HarvestHero" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Mengatur warna status bar -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Menyesuaikan teks status bar untuk warna terang -->
    </style>



    <style name="Theme.HarvestHero" parent="Base.Theme.HarvestHero" />
</resources>